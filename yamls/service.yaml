apiVersion: v1
kind: Service
metadata:
  name: servicio-inventario
  namespace: default
  labels:
    app: inventario-api
spec:
  type: NodePort
  ports:
    - name: http
      port: 80
      targetPort: 8000
      nodePort: 30800
  selector:
    app: inventario-api